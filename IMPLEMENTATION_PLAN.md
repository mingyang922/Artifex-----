# 面向游戏美术的大模型辅助资产生成平台 — 实现与改进计划

> 对照策划书要求与现有代码，制定的分阶段实现/改进计划（可直接按任务落地开发）。

---

## 一、策划书要求与现状对照

### 1.1 项目定位（策划书）

| 策划书要求                 | 现有实现                                          | 状态      |
| -------------------------- | ------------------------------------------------- | --------- |
| 面向游戏美术的 2D 资产生成 | 已有「AI 游戏资产生成器」、游戏资产类型与风格预设 | ✅ 已对齐 |
| 生成 UI、人物、场景素材    | UI/代码生成有；图片生成含角色/场景/道具/UI 类型   | ✅ 已覆盖 |
| 风格统一、低成本、可修改   | 风格预设、提示词模板有；统一性与可修改仍偏弱      | 🟡 部分   |

### 1.2 策划书中的「目前想要做的方向」

| 方向                           | 策划书描述                                    | 现有代码                                         | 建议                                                     |
| ------------------------------ | --------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------- |
| **一、UI 模型**                | 是否保留未定；或做成小功能，调用 Figma 大模型 | 有「代码生成」Tab（混元生成 HTML/CSS），无 Figma | 保留现有 UI 代码生成；可选：增加「Figma 对接」入口或说明 |
| **二、草图线稿→成品图**        | 类似 Holopix：线稿/草图生成成品图             | 无                                               | 新增「线稿转成图」模块（见阶段二）                       |
| **三、角色一致性**             | 角色 embedding、三视图→动作组、多图统一控制   | 无                                               | 中后期功能，先做「角色参考图+提示词」简化版（见阶段三）  |
| **四、风格 LoRA / 画风风格化** | 上传约 20 张同画师图→自动训练 LoRA→同风格生成 | 无                                               | 依赖训练与算力，建议列为远期或「说明页」                 |

### 1.3 策划书中的「遇到的问题」

| 问题                    | 在实现计划中的体现                                     |
| ----------------------- | ------------------------------------------------------ |
| **成本（3000 元经费）** | 优先用免费/低成本 API；控制调用次数；本地/轻量部署建议 |
| **可行性与时间**        | 分阶段：先做不依赖训练的功能，再考虑 embedding/LoRA    |

---

## 二、现有代码结构速览

```
GameManagement-platform/
├── backend/proxy.js          # 混元对话代理、多厂商文生图代理
├── config/api-config.js      # API 配置
├── dashboard.html            # 主控制台
├── login.html                # 登录（前端模拟）
├── modules/
│   ├── ai-generate/ai-generator-new.html   # AI 生成（代码+图片，游戏资产类型/风格/提示词模板/一键入素材库）
│   ├── asset-library/asset-library.html    # 素材库（分类含角色/场景/道具/UI，支持 imageUrl）
│   ├── project-management/                  # 项目管理
│   └── user-center/userCenter.html         # 用户中心
└── js/                      # 导航、头像、通知
```

**已有且与策划书一致的部分：**

- 游戏资产类型：角色立绘、场景概念图、道具/技能图标、UI 等
- 游戏风格预设：二次元、像素、赛博朋克、国风等
- 提示词模板 + 一键加入素材库
- 素材库分类与 imageUrl 支持

**尚未实现（策划书明确提到的）：**

- 线稿/草图 → 成品图（图生图）
- 角色一致性（embedding / 三视图→动作组）
- 风格 LoRA 或「上传多图→同风格生成」

---

## 三、分阶段实现/改进计划

### 阶段一：巩固现有能力与成本控制（优先做）

**目标**：在不增加大模型种类的前提下，把现有功能做稳，并控制调用成本、方便答辩/演示。

| 序号 | 任务                                 | 涉及文件/位置                                     | 说明                                                                                          |
| ---- | ------------------------------------ | ------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 1.1  | 移除后端硬编码密钥                   | `backend/proxy.js`                                | 腾讯 tencent 的 secretId/secretKey 仅从环境变量读取，删除默认占位值                           |
| 1.2  | Dashboard 最近项目用真实数据         | `dashboard.html`、新建 `js/dashboard-data.js`     | 从 `localStorage.gameui-projects` 取最近 3～5 条，替换静态卡片                                |
| 1.3  | 统一「生成记录」与「素材库」的数据键 | 全局约定                                          | 生成记录与素材库已用不同 key；保持现状即可，可在 README 注明                                  |
| 1.4  | 调用次数/配额提示（可选）            | `modules/ai-generate/ai-generator-new.html`、后端 | 前端在生成前可提示「注意 API 调用次数」；后端可加简单计数（内存或 JSON 文件）便于控制成本     |
| 1.5  | README 与策划书对齐                  | `README.md`                                       | 补充：面向游戏美术、2D 素材（UI/角色/场景）、现有功能列表、环境变量说明、成本与可行性简要说明 |

**交付物**：无新增大模型依赖；演示与答辩更稳；成本可控。

---

### 阶段二：线稿/草图 → 成品图（图生图）

**目标**：对应策划书「草图线稿生成成品图」，用现有或低成本图生图 API 做一版可用功能。

| 序号 | 任务                   | 涉及文件/位置                                | 说明                                                                                                                                                                 |
| ---- | ---------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1  | 后端图生图代理         | `backend/proxy.js`                           | 新增 `/api/image-to-image-proxy`，请求体含：base64 或 URL 的线稿/草图 + 文本 prompt；转发到腾讯/其他支持图生图的 API（需查当前腾讯云是否支持，或换一家低成本图生图） |
| 2.2  | 前端「线稿转成图」入口 | `modules/ai-generate/ai-generator-new.html`  | 新增 Tab 或区块：「线稿/草图转成图」；上传一张图 + 输入描述；调用 2.1 的接口；结果可预览、下载、一键加入素材库（复用现有逻辑）                                       |
| 2.3  | 结果写入素材库         | 复用 `addToAssetLibrary` 与素材库 `imageUrl` | 生成结果若为 URL，直接加入素材库；若为 base64，可转 dataURL 存入现有结构                                                                                             |

**依赖与成本**：依赖一家支持图生图的 API（腾讯云或其它）；在 3000 元内需控制调用次数与单价。

**交付物**：用户可上传线稿/草图，得到一张「成品风格」图，并进入素材库。

---

### 阶段三：角色一致性（简化版）

**目标**：不实现完整 embedding 管线，先做「多张参考图 + 文本描述」生成，为后续「三视图→动作组」打基础。

| 序号 | 任务                   | 涉及文件/位置                               | 说明                                                                                                                                                                      |
| ---- | ---------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1  | 「角色参考」生成模式   | `modules/ai-generate/ai-generator-new.html` | 新增模式：上传 1～3 张角色参考图（可选）+ 文本描述（动作/服装/场景）；后端将多图与文本一起组包发给多模态 API（若混元支持多图则用混元，否则用支持多图的文生图/图生图 API） |
| 3.2  | 后端多图 + 文本接口    | `backend/proxy.js`                          | 新路由，接收多图（base64 或 URL）+ prompt，转发到所选大模型；返回生成图 URL 或 base64                                                                                     |
| 3.3  | 结果与素材库、项目关联 | 同阶段二                                    | 生成结果可加入素材库；若有「当前项目」概念，可写入项目关联（若项目模块已支持关联素材则复用）                                                                              |

**说明**：完整「角色 embedding + 三视图→动作组」需要模型侧能力与算力，本阶段仅做「参考图+描述」的简化版，便于答辩展示方向。

**交付物**：用户可上传少量角色参考图并输入描述，生成一张尽量保持角色感的图，并进入素材库。

---

### 阶段四：UI 与 Figma（可选）

**目标**：对应策划书「UI 模型 / 调用 Figma 大模型」的犹豫，给出低成本可选方案。

| 序号 | 任务                   | 涉及文件/位置                             | 说明                                                                                                                       |
| ---- | ---------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 4.1  | 保留现有 UI 代码生成   | 无改动                                    | 继续使用混元生成 HTML/CSS，作为「轻量 UI 原型」能力                                                                        |
| 4.2  | Figma 对接（可选）     | 新页面或 Tab、后端新路由                  | 若选用 Figma API：后端仅做代理与鉴权，前端提供「描述→生成 UI」或「跳转 Figma 编辑」的入口；需评估 Figma 定价与 3000 元预算 |
| 4.3  | 或「Figma 使用说明」页 | 如 `modules/ai-generate/figma-guide.html` | 不接 API，仅文档说明：推荐用 Figma 大模型做 UI，本平台侧重角色/场景/道具与素材管理                                         |

**交付物**：要么有一个可演示的 Figma 小功能，要么有一份清晰的说明页，方便答辩时解释「UI 方向」的选择。

---

### 阶段五：风格 LoRA / 画风风格化（远期或说明）

**目标**：策划书提到「上传 20 张同画师图→自动训练 LoRA→同风格生成」；实现成本高，建议列为远期或仅做说明。

| 序号 | 任务            | 说明                                                                                                               |
| ---- | --------------- | ------------------------------------------------------------------------------------------------------------------ |
| 5.1  | 产品/技术说明页 | 在 README 或单独 doc 中说明：风格 LoRA 为远期规划，需训练环境与算力；当前通过「风格预设 + 提示词模板」尽量统一画风 |
| 5.2  | 若资源允许      | 可接入支持「风格参考」的第三方 API（若有），仅上传 1 张风格图 + prompt，不做自训练 LoRA                            |

**交付物**：文档上对齐策划书；若不做自训练 LoRA，不影响当前答辩与演示。

---

## 四、实施优先级与时间建议

| 优先级 | 阶段                     | 建议时间 | 备注                            |
| ------ | ------------------------ | -------- | ------------------------------- |
| P0     | 阶段一（巩固与成本）     | 1～2 天  | 立即做，无新 API 依赖           |
| P1     | 阶段二（线稿→成图）      | 3～5 天  | 需选定图生图 API 并做前后端联调 |
| P2     | 阶段三（角色参考简化版） | 3～5 天  | 依赖多模态/多图 API             |
| P3     | 阶段四（UI/Figma）       | 1～2 天  | 二选一：小功能或说明页          |
| P4     | 阶段五（LoRA/说明）      | 0.5 天   | 以文档为主                      |

---

## 五、成本与可行性简要（对应策划书）

- **大模型调用成本**：优先使用现有腾讯云混元/文生图；新增能力优先选按量付费、有免费额度的接口；在 README 或配置中注明「建议设置月度上限」。
- **数据库与服务器**：当前以 localStorage 为主，不增加后端数据库即可完成阶段一～三；若后期要做用户系统与持久化，再考虑 SQLite/免费云数据库。
- **可行性**：阶段一、二、三在现有技术栈下可实现；角色一致性的「完整版」和 LoRA 自训练可作为答辩中的「未来工作」说明。

---

## 六、需要学习/补充的知识（对应策划书「我们小组还要学哪些」）

- **若做线稿→成图**：所选图生图 API 的入参格式（base64/URL）、限流与计费方式。
- **若做角色参考多图**：多模态请求体构造、多图与文本的组合方式。
- **若做 Figma 对接**：Figma REST/插件 API、OAuth 或 API Key 管理。
- **通用**：环境变量与密钥管理、简单请求计数与限流，便于控制成本。

---

_本计划直接对照策划书与当前仓库代码制定，可按阶段拆成 Issue 或任务清单逐项实现。_
