# 面向游戏美术的大模型辅助资产生成平台 — 实现与改进计划

> 对照策划书要求与现有代码，制定的分阶段实现/改进计划（可直接按任务落地开发）。

---

## 一、策划书要求与现状对照

### 1.1 项目定位（策划书）

| 策划书要求                 | 现有实现                                          | 状态      |
| -------------------------- | ------------------------------------------------- | --------- |
| 面向游戏美术的 2D 资产生成 | 已有「AI 游戏资产生成器」、游戏资产类型与风格预设 | ✅ 已对齐 |
| 生成 UI、人物、场景素材    | UI/代码生成有；图片生成含角色/场景/道具/UI 类型   | ✅ 已覆盖 |
| 风格统一、低成本、可修改   | 风格预设、提示词模板有；统一性与可修改仍偏弱      | 🟡 部分   |

### 1.2 策划书中的「目前想要做的方向」

| 方向                           | 策划书描述                                    | 现有代码                                         | 建议                                                     |
| ------------------------------ | --------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------- |
| **一、UI 模型**                | 是否保留未定；或做成小功能，调用 Figma 大模型 | 有「代码生成」Tab（混元生成 HTML/CSS），无 Figma | 保留现有 UI 代码生成；可选：增加「Figma 对接」入口或说明 |
| **二、草图线稿→成品图**        | 类似 Holopix：线稿/草图生成成品图             | 无                                               | 新增「线稿转成图」模块（见阶段二）                       |
| **三、角色一致性**             | 角色 embedding、三视图→动作组、多图统一控制   | 无                                               | 中后期功能，先做「角色参考图+提示词」简化版（见阶段三）  |
| **四、风格 LoRA / 画风风格化** | 上传约 20 张同画师图→自动训练 LoRA→同风格生成 | 无                                               | 依赖训练与算力，建议列为远期或「说明页」                 |

### 1.3 策划书中的「遇到的问题」

| 问题                    | 在实现计划中的体现                                     |
| ----------------------- | ------------------------------------------------------ |
| **成本（3000 元经费）** | 优先用免费/低成本 API；控制调用次数；本地/轻量部署建议 |
| **可行性与时间**        | 分阶段：先做不依赖训练的功能，再考虑 embedding/LoRA    |

---

## 二、现有代码结构速览（v1.3.3 更新）

```
Artifex/
├── backend/
│   ├── proxy.js / proxy.ts          # 服务入口、Session、中间件、路由注册
│   ├── routes/                      # auth / image-proxy / projects / asset-library / admin / ai-providers
│   ├── providers/                   # tencent / alibaba / jimeng / sd-webui
│   ├── lib/                         # utils / auth-policy / rate-limiter / logger / ws-server / user-api-settings
│   └── db/users-db.js               # SQLite 数据库（用户、项目、素材、活动日志、用量追踪）
├── modules/
│   ├── ai-generate/                 # AI 生成器（文生图/图生图/风格迁移/放大/去背/动作组）
│   ├── asset-library/               # 素材库（标签/搜索/筛选/批量操作/URL导入）
│   ├── project-management/          # 项目管理（版本控制/模板/导出）
│   ├── user-center/                 # 用户中心（密码安全/API凭证/多语言/活动日志）
│   ├── style-presets/               # 风格预设
│   ├── admin/                       # 管理员面板
│   └── message-center.html          # 消息中心
├── js/                              # 共享脚本（i18n / ui-kit / navigation / notification / ws-client 等）
├── styles/                          # 共享样式
├── vendor/                          # 本地化 CDN 资源（Font Awesome / Google Fonts / JSZip）
├── tests/ + e2e/                    # 单元测试 + Playwright E2E 测试
├── sw.js + manifest.json            # PWA 支持
├── vite.config.js + tsconfig.json   # 构建工具 + TypeScript 配置
└── docs/                            # 用户手册、安全审计、性能基准、源代码文档、OpenAPI
```

**已有且与策划书一致的部分：**

- 游戏资产类型：角色立绘、场景概念图、道具/技能图标、UI 等
- 游戏风格预设：二次元、像素、赛博朋克、国风等
- 提示词模板 + 一键加入素材库
- 素材库分类与 imageUrl 支持
- AI 图生图（风格迁移、参考图重绘）
- 角色动作组三视图批量生成
- 游戏引擎导出（Unity / Godot / 通用 SpriteSheet）

**已实现的额外工程化能力（策划书未要求）：**

- 用户认证系统（注册/登录/会话 Cookie/bcrypt 密码哈希）
- SQLite 数据库（WAL 模式、预编译语句、级联删除）
- 安全加固（AES-256-GCM、CSRF、SSRF 防护、CSP、限流）
- PWA 离线支持 + Service Worker 缓存策略
- WebSocket 实时通知
- 多语言（中/繁中/英/日/韩）
- TypeScript 迁移（后端 21 个文件，strict 模式）
- E2E 测试（23 个 Playwright 用例）
- Docker 多阶段构建 + CI/CD（GitHub Actions）
- Vite 打包 + 代码分割 + script defer 优化

**尚未实现（策划书明确提到的）：**

- 专属「线稿/草图→成品图」工作流（图生图能力已有，但 UI 未单独封装）
- 完整角色一致性（embedding / 多图参考→统一生成）
- 风格 LoRA / 上传多图→同风格训练

---

## 三、分阶段实现/改进计划

### 阶段一：巩固现有能力与成本控制 ✅ 已完成

**目标**：在不增加大模型种类的前提下，把现有功能做稳，并控制调用成本、方便答辩/演示。

| 序号 | 任务                                 | 状态 | 说明                                                        |
| ---- | ------------------------------------ | ---- | ----------------------------------------------------------- |
| 1.1  | 移除后端硬编码密钥                   | ✅   | API 密钥从环境变量 / 用户凭证读取，AES-256-GCM 加密存储     |
| 1.2  | Dashboard 最近项目用真实数据         | ✅   | 从 SQLite 数据库加载，API 分页查询                          |
| 1.3  | 统一「生成记录」与「素材库」的数据键 | ✅   | 素材库已迁移至服务端 SQLite，统一管理                       |
| 1.4  | 调用次数/配额提示                    | ✅   | 后端 `usage_logs` 表 + 复合索引，管理员配额管理面板         |
| 1.5  | README 与策划书对齐                  | ✅   | README 已更新，补充面向游戏美术定位、功能列表、环境变量说明 |

**交付物**：无新增大模型依赖；SQLite 数据库、用户认证、CSRF/SSRF 防护、限流均已落地。

---

### 阶段二：线稿/草图 → 成品图（图生图） 🟡 部分完成

**目标**：对应策划书「草图线稿生成成品图」，用现有或低成本图生图 API 做一版可用功能。

| 序号 | 任务                   | 状态 | 说明                                                                             |
| ---- | ---------------------- | ---- | -------------------------------------------------------------------------------- |
| 2.1  | 后端图生图代理         | ✅   | `image-proxy.js` 支持图生图，腾讯云/阿里云/即梦均有图生图接口                    |
| 2.2  | 前端「线稿转成图」入口 | 🟡   | AI 生成器支持图生图模式（上传参考图 + prompt），但无专门的「线稿→成品图」UI 入口 |
| 2.3  | 结果写入素材库         | ✅   | 生成结果可一键加入素材库                                                         |

**说明**：图生图能力已通过「AI 风格迁移」和「AI 重绘」实现，但策划书描述的「线稿/草图→成品图」专属工作流尚未单独封装。可在现有 AI 生成器中新增「线稿转成图」Tab。

---

### 阶段三：角色一致性（简化版） 🟡 部分完成

**目标**：不实现完整 embedding 管线，先做「多张参考图 + 文本描述」生成，为后续「三视图→动作组」打基础。

| 序号 | 任务                   | 状态 | 说明                                                  |
| ---- | ---------------------- | ---- | ----------------------------------------------------- |
| 3.1  | 「角色参考」生成模式   | 🟡   | 图生图支持上传参考图，但无专门的「多参考图+描述」模式 |
| 3.2  | 后端多图 + 文本接口    | 🟡   | 各 provider 支持图生图（单图），但无多图+文本组合接口 |
| 3.3  | 结果与素材库、项目关联 | ✅   | 生成结果可加入素材库并关联项目                        |

**已实现**：角色动作组三视图生成（`action-group.js`），可批量生成角色不同角度/动作。

**未实现**：完整「角色 embedding + 多图参考→一致性生成」需模型侧支持，可作为未来工作。

---

### 阶段四：UI 与 Figma（可选）

**目标**：对应策划书「UI 模型 / 调用 Figma 大模型」的犹豫，给出低成本可选方案。

| 序号 | 任务                   | 涉及文件/位置                             | 说明                                                                                                                       |
| ---- | ---------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 4.1  | 保留现有 UI 代码生成   | 无改动                                    | 继续使用混元生成 HTML/CSS，作为「轻量 UI 原型」能力                                                                        |
| 4.2  | Figma 对接（可选）     | 新页面或 Tab、后端新路由                  | 若选用 Figma API：后端仅做代理与鉴权，前端提供「描述→生成 UI」或「跳转 Figma 编辑」的入口；需评估 Figma 定价与 3000 元预算 |
| 4.3  | 或「Figma 使用说明」页 | 如 `modules/ai-generate/figma-guide.html` | 不接 API，仅文档说明：推荐用 Figma 大模型做 UI，本平台侧重角色/场景/道具与素材管理                                         |

**交付物**：要么有一个可演示的 Figma 小功能，要么有一份清晰的说明页，方便答辩时解释「UI 方向」的选择。

---

### 阶段五：风格 LoRA / 画风风格化（远期或说明）

**目标**：策划书提到「上传 20 张同画师图→自动训练 LoRA→同风格生成」；实现成本高，建议列为远期或仅做说明。

| 序号 | 任务            | 说明                                                                                                               |
| ---- | --------------- | ------------------------------------------------------------------------------------------------------------------ |
| 5.1  | 产品/技术说明页 | 在 README 或单独 doc 中说明：风格 LoRA 为远期规划，需训练环境与算力；当前通过「风格预设 + 提示词模板」尽量统一画风 |
| 5.2  | 若资源允许      | 可接入支持「风格参考」的第三方 API（若有），仅上传 1 张风格图 + prompt，不做自训练 LoRA                            |

**交付物**：文档上对齐策划书；若不做自训练 LoRA，不影响当前答辩与演示。

---

## 四、实施优先级与时间建议

| 优先级 | 阶段                     | 建议时间 | 备注                            |
| ------ | ------------------------ | -------- | ------------------------------- |
| P0     | 阶段一（巩固与成本）     | 1～2 天  | 立即做，无新 API 依赖           |
| P1     | 阶段二（线稿→成图）      | 3～5 天  | 需选定图生图 API 并做前后端联调 |
| P2     | 阶段三（角色参考简化版） | 3～5 天  | 依赖多模态/多图 API             |
| P3     | 阶段四（UI/Figma）       | 1～2 天  | 二选一：小功能或说明页          |
| P4     | 阶段五（LoRA/说明）      | 0.5 天   | 以文档为主                      |

---

## 五、成本与可行性简要（对应策划书）

- **大模型调用成本**：腾讯云混元/文生图、阿里云万相、即梦 Seedream 均已接入；用户可自配 API 凭证，控制个人调用量；管理员可使用平台默认密钥。
- **数据库与服务器**：已采用 SQLite（`better-sqlite3`），支持 WAL 模式、预编译语句缓存、复合索引优化。已部署至腾讯云服务器（artifex.com.cn），Nginx 反向代理 + PM2 进程管理。
- **可行性**：阶段一已全部完成；阶段二、三部分完成（图生图、动作组已落地）；LoRA 自训练、Figma 对接可作为「未来工作」说明。

---

## 六、需要学习/补充的知识（对应策划书「我们小组还要学哪些」）

- **若做线稿→成图**：所选图生图 API 的入参格式（base64/URL）、限流与计费方式。
- **若做角色参考多图**：多模态请求体构造、多图与文本的组合方式。
- **若做 Figma 对接**：Figma REST/插件 API、OAuth 或 API Key 管理。
- **通用**：环境变量与密钥管理、简单请求计数与限流，便于控制成本。

---

_本计划直接对照策划书与当前仓库代码制定，可按阶段拆成 Issue 或任务清单逐项实现。_
