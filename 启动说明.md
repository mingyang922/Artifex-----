# 启动说明

本说明用于快速将项目跑起来。更详细的模块介绍请见 [`README.md`](./README.md)。

## 最快体验方式

无需安装任何环境，直接访问已部署的云端站点即可：

- <https://artifex.com.cn/login.html>

若需要在本地运行整套项目（例如审阅源码），继续阅读下文。

---

## 目录

- [零、Windows 一键启动](#零windows-一键启动)
- [一、环境准备](#一环境准备)
- [二、安装依赖](#二安装依赖)
- [三、可选配置](#三可选配置)
- [四、启动服务](#四启动服务)
- [五、访问平台](#五访问平台)
- [六、停止与重启](#六停止与重启)
- [七、启动失败排查](#七启动失败排查)

## 零、Windows 一键启动

项目根目录提供了 `一键启动.cmd`，双击即可自动完成依赖安装、兼容性检查、后端启动和登录页打开。另提供了第二种终端启动方式 `start-workbench.bat`，和一键启动共用同一套跨机器逻辑。脚本内部会按顺序执行：

1. 检测 Node.js / npm 是否安装，并校验主版本为 **22.x**（与 `.nvmrc` 一致）。
2. 若 `node_modules` 不存在则执行 `npm install`（`postinstall` 会自动重编 `better-sqlite3`）。
3. 检查 `better-sqlite3` 原生模块是否可用，不可用会自动 `npm rebuild` 修复。
4. 启动后端，等待健康检查通过后自动弹出浏览器打开 <http://localhost:3000/login.html>。

启动日志写入 `logs/backend-launch.log`，启动失败时可优先查看。

若窗口长时间停在「等待后端健康检查」：说明后端尚未对 `/api/health` 返回成功；请打开 **`logs/backend-launch.log`** 看末尾错误（一键脚本约每 15 秒会在窗口打印日志尾部）。仍无法解决时，请改用下文「四、启动服务」中的 **`npm run start:open`**、**`start-workbench.bat`** 或 **前台 `npm start`** 排障。

> 非 Windows 用户或需要自定义配置时，请按下文「一～五」手动启动。

---

## 一、环境准备

- 安装 **Node.js 22 LTS**（推荐 `22.22.0`，见根目录 `.nvmrc`）。
- 自带的 **npm** 版本请保持在 **10** 及以上。
- 使用 nvm-windows：`nvm install 22.22.0` → `nvm use 22.22.0`。
- 操作系统 Windows / macOS / Linux 均可。

验证安装：

```bash
node -v
npm -v
```

## 二、安装依赖

在项目根目录执行：

```bash
npm install
```

> 如遇 `better-sqlite3` 安装失败，请先安装本机的 C/C++ 编译环境后重试：Windows 推荐安装 Visual Studio Build Tools（勾选「使用 C++ 的桌面开发」），macOS 执行 `xcode-select --install`，Linux 安装 `build-essential`、`python3`。

## 三、可选配置

首次启动可以不做任何配置，默认使用端口 `3000`。如需自定义端口、会话密钥或跨域来源，可按以下步骤生成本地环境文件：

**Windows PowerShell**

```powershell
Copy-Item backend\.env.example backend\.env
```

**macOS / Linux**

```bash
cp backend/.env.example backend/.env
```

随后按 `backend/.env` 中的注释进行编辑即可。该文件仅保存在本地

## 四、启动服务

项目提供以下几种启动方式，任选其一：

```bash
# 生产方式启动
npm start

# 启动后端并在健康检查通过后自动打开登录页（需已 npm install）
npm run start:open

# Windows 终端启动脚本，逻辑和一键启动一致
start-workbench.bat

# 开发模式（修改代码后自动重启）
npm run dev

# 直接用 node 启动
node backend/proxy.js
```

本地调试若已全局设置 `NODE_ENV=production`，请在前台启动时显式指定开发环境，例如：

```bat
set NODE_ENV=development
npm start
```

启动成功后，终端会输出类似「Server listening on 3000」或相关环境加载信息。

## 五、访问平台

**本地访问**（本机启动后端后）：

- 登录页：<http://localhost:3000/login.html>
- 主控制台：<http://localhost:3000/dashboard.html>

**云端访问**（已部署，无需本地环境）：

- 登录页：<https://artifex.com.cn/login.html>
- 主控制台：<https://artifex.com.cn/dashboard.html>

访问根地址会自动跳转到登录页。首次使用请先通过登录页的「立即注册」创建账号。

> 请务必通过 `http://` 域名或 `localhost` 访问，不要直接用浏览器打开 HTML 文件（`file://`），否则会因同源策略导致登录接口不可用。

## 六、停止与重启

- 停止：在运行服务的终端按下 `Ctrl + C`。
- 重启：停止后再次执行上述启动命令；`npm run dev` 模式会在保存代码时自动重启。

## 七、启动失败排查

| 现象                                    | 处理建议                                                                                                                                                             |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 端口 3000 被占用                        | 在 `backend/.env` 中设置 `PORT=其他端口`，重启后使用新地址访问                                                                                                       |
| `better-sqlite3` 安装失败               | 参考「二、安装依赖」中的编译环境提示，装好工具链后删除 `node_modules` 重新安装                                                                                       |
| 后端启动报 `NODE_MODULE_VERSION` 不匹配 | 确认 `node -v` 为 **22.x**（`nvm use 22.22.0`），删除 `node_modules` 后执行 `npm install`；或单独执行 `npm rebuild better-sqlite3`                                                                 |
| 一键启动卡在「等待后端健康检查」        | 打开 `logs/backend-launch.log`；主窗口约每 15 秒会打印日志尾部；或改用 `npm run start:open` / `start-workbench.bat` / 前台 `set NODE_ENV=development` 后 `npm start` |
| 登录页显示"无法连接服务器"              | 确认后端已启动、端口一致、并通过 `http://localhost:...` 访问                                                                                                         |
| 页面样式异常或资源 404                  | 确认未删改 `styles/`、`js/` 目录结构，清理浏览器缓存后再刷新                                                                                                         |
| 控制台报跨域错误                        | 检查 `ALLOWED_ORIGINS` 是否包含当前访问的域名/端口                                                                                                                   |

### 上传部署后无法登录（云端 502）

**现象**：`https://artifex.com.cn/login.html` 能打开，但登录提示失败；`/api/health` 或 `/api/auth/login` 返回 **502 Bad Gateway**。

**原因**：Nginx 反代不到本机 `3000` 端口，通常是 **Node 后端未启动或启动后立刻退出**，不是密码错误。

**在服务器 SSH 依次执行**：

```bash
pm2 status
pm2 logs game-management-backend --lines 40
curl -s http://127.0.0.1:3000/api/health
```

若 `curl` 失败，检查 `/srv/game-management/current/backend/.env`（路径按你机器实际为准）：

1. **`NODE_ENV=production` 时必须同时有** `SESSION_SECRET`（非空）和 `ALLOWED_ORIGINS`（含 `https://artifex.com.cn`），缺一后端会直接退出。可参考 `backend/.env.production.example` 与 `docs/DOMAIN_artifex.com.cn.md`。
2. **不要用本机开发包覆盖** `backend/data/users.sqlite`，否则旧账号消失；部署前应备份并恢复用户库。
3. 在 `backend` 目录执行 `npm install` 后 `pm2 restart game-management-backend`；若日志含 `better-sqlite3`，在服务器安装 `build-essential` 后 `npm rebuild better-sqlite3`。

`curl http://127.0.0.1:3000/api/health` 返回 JSON 后，再在浏览器登录；若提示「邮箱或密码错误」，用注册新账号或恢复备份的 `users.sqlite`。

如仍无法启动，可将终端与浏览器控制台的报错信息整理后反馈给项目维护者。
