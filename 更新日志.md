# 更新日志

## 2026-06-16
### 修复
- **asset-library 标签筛选失效**：`asset-library-filter.js` 的 `activeTagFilter` 为常量 `null`，导致标签筛选 UI 有响应但实际不过滤。修复：`asset-library.js` 通过 `tagFilter` 参数传递当前筛选值。
- **asset-library-filter.js 死代码清理**：移除未使用的 `renderTagFilterBar`、`showCardContextMenu`、`showProjectSelector`、`showTagEditor`、`enhanceRenderedCards`、`init` 等函数（310 行 → 56 行），仅保留纯逻辑 `filterAssets`。
- **main.js 语法错误**：`const newProject;` 缺少初始化器，改为 `let newProject;`。
- **utils.js decryptText 变量名不匹配**：catch 块中 `e.message` 应为 `_e.message`。

### 重构
- **asset-library 上下文菜单拆分**：`showCardContextMenu`、`showProjectSelector`、`showTagEditor`、`saveAssetTags` 提取至 `asset-library-context-menu.js`（280 行），通过 `window.AssetLibraryContextMenu` 暴露，依赖注入模式。
- **asset-library.js 瘦身**：从 89KB 降至 79KB。

### 测试
- **新增 utils.test.js**：25 个测试用例，覆盖加密/解密往返、篡改检测、unicode 支持、敏感字段加解密、即梦 API Key 规范化、像素尺寸解析、腾讯云凭证校验、即梦响应解析。
- **测试总量**：78 个用例，全部通过。

### 工程化
- **统一错误响应格式**：新增 `backend/lib/error-response.js`，定义标准格式 `{ error: { code, message, details? } }` 和错误码常量（ERR_VALIDATION、ERR_AUTH_REQUIRED 等）。路由可渐进采用。

### 代码质量
- **var 全部改为 const/let**：142 处 `var` 声明统一改为 `const`/`let`，符合现代 JavaScript 规范。
- **jimeng.js 严格比较**：5 处 `!=` 改为 `!==`，与项目规范一致。
- **ESLint warnings 清理**：从 199 个降至 145 个（剩余为 catch 块中故意未使用的变量）。
- **session-guard.js 优化**：扩展静默路径（`/api/csrf-token`、`/api/health`），减少误拦截。

### 重构
- **ImageGenerator 模块拆分（第一阶段）**：
  - 新增 `image-storage.js`（存储和持久化逻辑，375 行）
  - 新增 `image-style-presets.js`（风格预设管理，245 行）
  - 主文件构造函数初始化拆分模块
- **会话存储改为 SQLite**：`connect-sqlite3` 替代内存存储，PM2 重启不再丢失用户会话。
- **TypeScript 回退**：经评估，TS 迁移对本项目弊大于利（维护双版本、依赖兼容问题），回退为纯 JavaScript。

### 修复
- `auth.js` 修复 3 处 catch 变量名不匹配（`_e` vs `e`），错误日志现在能正确输出。
- `image-style-presets.js` 修复 `const data;` 语法错误（应为 `let data;`）。
- `backend/db/users-db.js` 修复 sed 误改 SQL 语句中的 `!=` 为 `!==`。
- 清理 agent 遗留临时脚本（fix-*.cjs、lint-out*.txt）。

### 清理
- 删除无用 Python 迁移脚本（strip-inline-ai-script.py、strip-usercenter-script.py）。
- 软件说明书移除 TypeScript 引用。
- `.gitignore` 更新，排除 `test-results/` 和 `*.tar.gz`。

## 2026-06-14
### 优化
- `.gitignore` 新增排除 `modules/asset-library/素材库图片/`，测试素材图片不再纳入版本控制。
- `IMPLEMENTATION_PLAN.md` 更新：阶段一（巩固与成本）全部标记为已完成；阶段二（线稿→成图）、阶段三（角色一致性）部分功能已实现；补充当前代码结构、已完成的工程化改进（SQLite、PWA、WebSocket、TypeScript、E2E 测试等）；更新「尚未实现」清单。
- `visual-workbench.html` 标记为遗留重定向页，仅作向后兼容保留。

## 2026-06-08
### 新增
- 备案信息底部栏：11 个页面添加公安备案（京公网安备11010802048813号）和 ICP 备案（京ICP备2026031602号），毛玻璃固定底栏 + 备案图标。
- 繁体中文（zht）多语言支持：87 个翻译条目全部添加繁体版本，用户中心语言选择器新增「繁體中文」选项。
- `backend/db/users-db.js` 新增 `close()` 函数，支持优雅关闭时数据库连接清理。
- `backend/lib/rate-limiter.js` 新增 `destroy()` 方法和 `unref()` 防止定时器阻塞进程退出。
- `js/sw-register.js` 新增 Service Worker 更新检测（`updatefound` 事件）和定期检查（30 分钟）。

### 安全修复
- **AES-256-GCM 加密升级**：`backend/lib/utils.js` 存储 API 密钥从 AES-256-CBC（无认证）升级为 AES-256-GCM（带 AuthTag），向后兼容旧格式；`decryptText` 失败返回空字符串而非密文原文。
- **SSRF 防护增强**：`backend/routes/image-proxy.js` DNS rebinding 防护改用 `dns.lookup` 同时解析 IPv4/IPv6；修正 IPv6 正则（移除括号）；DNS 解析失败时阻止请求而非放行；添加 `maxContentLength: 20MB` 限制；Content-Type 白名单仅允许图片类型。
- **Admin 权限修复**：`backend/lib/auth-policy.js` 移除 `profile_json` 回退检测，仅信任 `role` 列；`backend/db/users-db.js` `getAllUsers()` 移除 `profile.role` 回退。
- **登出修复**：`backend/routes/auth.js` `clearCookie` 传递完整 cookie 选项（`httpOnly/sameSite/secure/path`），修复生产环境 cookie 未清除问题。
- **用户名校验**：注册接口新增用户名格式校验（2-32 字符，字母/数字/下划线/中文）。
- **字段长度限制**：`backend/lib/user-api-settings.js` 凭证字段限制最大 4096 字符。
- **HTTP 方法修正**：`backend/routes/ai-providers.js` `jimeng/live-test` 从 GET 改为 POST（有副作用的请求）。
- **PM2 配置修正**：`backend/ecosystem.config.js` 移除无效的 `env_file` 选项，`max_memory_restart` 提升至 1024M。
- **Admin 用户可配置**：`ADMIN_USER_ID` 环境变量支持指定管理员用户 ID，首个注册用户自动成为管理员。

### 前端修复
- **localStorage 隔离修复**：`modules/ai-generate/image-generator.js` 4 处 `generatedImages` 写入改用 `aiStorageKey()` 隔离，修复多账号数据泄露。
- **语法错误修复**：`image-generator.js` 第 322 行孤立 `document` 表达式修复。
- **并发竞态修复**：`modules/ai-generate/action-group.js` 帧生成并发池索引改为原子计数器。
- **JSON.parse 安全**：`modules/user-center/user-center-page.js` 4 处 `JSON.parse` 添加 try-catch。
- **DOM null 守卫**：`modules/user-center/password-security.js` 所有 `getElementById` 添加 null 检查，初始化包裹在 `DOMContentLoaded` 中。
- **密码长度一致性**：`password-security.js` 强度检查统一为 ≥6 字符。
- **键盘快捷键修复**：`js/keyboard-shortcuts.js` Ctrl+S 和空格键仅在目标按钮存在时才阻止默认行为。
- **SW 离线回退**：`sw.js` 离线回退优先返回 dashboard 而非 login；缓存条目上限 200 条自动裁剪。
- **SW 更新提示**：`js/sw-register.js` 检测新版本时弹出 toast 提示用户刷新。
- **escapeHtml 回退**：`js/notification-component.js` 回退函数添加完整 HTML 转义。
- **GameUiUserScope 守卫**：`js/dashboard-init.js` 添加 typeof 检查，防止脚本加载顺序问题。
- **导入验证**：`js/data-portability.js` 添加 10MB 大小限制、键名安全检查。
- **路径修正**：`modules/messageCenter.html` 3 处资源路径从 `../../` 修正为 `../`。
- **素材库 null 守卫**：`modules/asset-library/asset-library.js` DOM 元素添加 null 检查，`window.open` 处理弹窗阻止回退。
- **编辑器内存优化**：`modules/ai-generate/asset-editor.js` 历史记录 `maxHistory` 从 40 降至 20，`generatedImages` 改用 scoped key。
- **action-group 并行化**：帧生成从串行改为并发 4 路，16 帧生成速度提升约 4 倍。

### 数据库
- `asset_library` 和 `activity_log` 外键添加 `ON DELETE CASCADE`，与其他表保持一致。
- `usage_logs` 添加复合索引 `(user_id, provider, created_at)`，配额检查查询提速。
- `getUserProjects()` 批量查询 assets 和 version_history，消除 N+1 问题（20 个项目从 41 次查询降至 3 次）。
- `getUserApiCredentialStatus()` 改为单次查询，消除 N+1 问题。
- `getProject()` 的 assets 和 version_history 添加 LIMIT（200/50），防止无限制返回。

### 性能优化
- 静态资源添加缓存头：生产环境 CSS/JS/图片缓存 7 天，HTML 文件 `no-cache`。
- Gzip 压缩跳过已压缩图片格式（JPEG/PNG/WebP/GIF），减少无效 CPU 开销。
- **script defer**：10 个 HTML 页面共 100+ 个 `<script>` 标签添加 `defer`，页面渲染不再等待 JS 加载。
- **SQL 预编译语句缓存**：`getUserById`、`getUserByEmail`、`logApiCall`、`checkQuota` 等 6 个高频查询的预编译语句提升为模块级变量，避免每次调用重复解析 SQL。
- **requireAuth 用户缓存**：认证中间件加 5 秒 TTL 缓存，典型页面加载从 5-10 次查询降至 1 次。
- **WAL checkpoint 定时化**：移除 15 处写操作后的同步 checkpoint，改为每 30 秒定时执行，减少事件循环阻塞。
- **Vite 构建优化**：添加 `target: es2020`、`minify: esbuild`、文件哈希（`[name]-[hash]`）、代码分割（`vendor-utils` chunk）。

### 安全加固
- CSP `imgSrc` 从 `https:`（允许所有 HTTPS 源）收紧为 `'self' data: blob:`，防止图片数据泄露。
- 修改密码接口添加专用限流（15 分钟内最多 5 次）。
- 登出接口添加 CSRF 防护。

### 新增功能
- **PWA 支持**：新增 `manifest.json`，应用可安装到桌面，支持离线使用。
- **WebSocket 实时通知**：新增 `backend/lib/ws-server.js` 和 `js/ws-client.js`，支持服务端推送通知。
- **客户端图片压缩**：新增 `js/image-compress.js`，上传前自动压缩大图，减少传输体积。
- **前端懒加载**：新增 `js/lazy-load.js`，支持按需加载 JS 模块。
- **E2E 测试**：新增 Playwright 端到端测试（12 个用例），覆盖认证、控制台、API 接口。
- **Docker 多阶段构建**：优化 `Dockerfile`，分离依赖安装、构建、运行三个阶段，镜像体积减小。
- **ES Module 迁移**：`js/constants.js` 和 `js/html-utils.js` 从 IIFE 转为 ES Module，支持 tree-shaking。
- ~~**TypeScript 迁移**~~：已回退。经评估，TypeScript 迁移对本项目弊大于利（维护双版本、依赖兼容问题），回退为纯 JavaScript，代码更简洁。
- **ES Module 迁移**：`js/constants.js`、`js/html-utils.js` 从 IIFE 转为 ES Module，支持 tree-shaking。
- **E2E 测试扩展**：新增 11 个测试用例（用户中心、素材库、风格预设、AI 生成器），总计 23 个 E2E 测试。
- **SW manifest 自动生成**：新增 `scripts/generate-sw-manifest.js`，Vite 构建后自动生成缓存清单。

### 文档
- **用户手册**：新增 `docs/用户手册.md`，包含完整的功能说明和操作指南。
- **性能基准文档**：新增 `docs/性能基准文档.md`，记录优化措施和基准测试数据。
- **安全审计文档**：新增 `docs/安全审计文档.md`，记录安全特性和审计结果。
- **源代码文档**：重新生成，36127 行 / 723 页。
- **软件说明书**：更新技术架构图和技术选型表（TypeScript、Playwright、Vite、WebSocket）。
- **README**：更新相关文档引用。

## 2026-06-05
### 新增
- Vite 打包工具集成：`vite.config.js` 多页面入口配置、开发服务器代理、`npm run build` / `npm run dev` / `npm run preview` 命令。
- Service Worker（PWA）：`sw.js` 静态资源缓存 + 离线回退，11 个页面注入注册脚本。
- `js/constants.js` 前端共享常量模块：集中管理魔法数字（即梦最低像素、生成图片缓存上限、文件大小限制等）。
- `js/html-utils.js` 共享 HTML 转义模块：统一 `escapeHtml` / `escapeJsStr`，消除 9 处重复定义。
- `modules/user-center/password-security.js`：从 `user-center-page.js`（2502 行）提取 `PasswordSecurityManager` 类（700 行）。
- `js/sw-register.js`：Service Worker 自动注册（仅 HTTPS / localhost）。
- `js/entry-login.js` / `js/entry-dashboard.js`：Vite 入口文件。

### 优化
- 18 个后端文件补 `'use strict'` 声明，全站 JS 文件严格模式覆盖率达 100%。
- 5 个文件（data-portability、dashboard-init、scroll-animations、keyboard-shortcuts、image-generator）共 63 处 `var` 改为 `let/const`。
- 素材库搜索加 300ms 防抖，避免大量素材时输入卡顿。
- AI 生成器 `persistGeneratorUiState` 加 300ms 防抖，避免高频写入 sessionStorage。
- 通知组件 `@keyframes spin` 改为一次性注入，避免 DOM 膨胀。
- 图片代理 `/api/proxy-image` 添加 `Cache-Control: public, max-age=3600` 缓存头。
- `jimeng.js` 尺寸校验 `!=` 改为 `!==`，明确类型语义。
- 项目详情页 3 张外部占位图（via.placeholder.com）改为本地 CSS 渐变 + 图标。
- `.gitignore` 添加 `dist/` 排除构建产物。
- 移除未使用的 `typescript` devDependency。

### 修复
- `image-generator.js` 构造函数 `JSON.parse` 加 try/catch 保护，防止 localStorage 损坏导致页面崩溃。
- `asset-editor.js` 同上。
- `image-generator.js` `previewImage` 检查 `window.open` 返回值，弹窗被阻止时提示用户。
- `image-generator.js` `handleError` 移除重复的重试按钮。
- `password-security.js` `res.json()` 加 `.catch(() => ({}))` 保护，防止服务器返回非 JSON 时页面崩溃。
- `dashboard-init.js` API 401 重定向到登录页，不再回退到过期的 localStorage 缓存。
- `auth.js` 敏感操作补审计日志：密码修改、API 密钥更新/删除。
- 前端 `console.warn`（面向用户场景）改为 `console.debug`，不再暴露内部日志。
- `style-presets.js` / `asset-library.js` 移除原生 `confirm()` / `prompt()` 回退，统一使用 TechUI。

### 测试
- `tests/backend.test.js`：从 27 → 37 个用例，新增项目 CRUD、素材库、活动日志、加密解密测试。
- `tests/routes.test.js`：从 5 → 16 个用例，新增路由模块导出、Helmet 安全头、压缩中间件测试。
- 总计 53 个测试用例，全部通过。

## 2026-06-04
### 新增
- 混元 LLM 对话代理改用腾讯云官方 SDK（`tencentcloud-sdk-nodejs`），替代手动 TC3 签名实现。
- 腾讯云 API 权限分级：管理员可直接使用平台默认密钥，普通用户须在「用户中心」自配 SecretId / SecretKey 后方可调用混元 LLM 和腾讯云图片生成。
- 腾讯云状态接口 `/api/tencent/status` 新增 `hasOwnKeys`、`usesPlatformKeys`、`isAdmin` 字段，前端可据此区分密钥来源并引导用户配置。

### 修复
- `generateSignature` 日期 bug：`new Date()` 改为 `new Date(timestamp * 1000)`，修复签名中 date 与 timestamp 不一致导致的 `AuthFailure.InvalidAuthorization`。
- `handleHunyuanProxy` 错误处理顺序 bug：`error.code === 'ECONNABORTED'` 超时判断移至 `error.code && error.message` 之前，修复超时错误被误报为 400 而非 408；新增 `ETIMEDOUT` 检测。
- 移除 `backend/providers/tencent.js` 中未使用的 `axios` 和 `generateSignature` 导入。

## 2026-06-03
### 新增
- AI 图片放大（2x/4x 超分辨率）。
- AI 背景去除（输出透明 PNG）。
- 素材跨项目复制（选择目标项目一键复制）。
- 素材标签系统（自定义标签 + 标签筛选）。
- 素材 URL 导入（输入图片链接直接导入）。
- 快捷键系统（Ctrl+S 保存、Ctrl+Enter 生成、Esc 关闭弹窗）。
- 批量 AI 生成（一次生成 1/2/4/8 张变体）。
- 项目模板（RPG/平台跳跃/消除游戏/UI 套件）。
- 相似素材检测（感知哈希对比，分组展示相似素材）。

### 优化
- 统一版本号管理：`api-config.js` 新增 `ARTIFEX_VERSION` 常量，启动动画与健康检查 API 动态读取。
- `saveProjectToServer` 改为发送 `versionDesc`，并从服务端响应同步版本号，消除前后端版本冲突。
- `saveEditProject` / `addProjectAsset` / 删除素材不再本地手动递增版本号，由服务端统一管理。
- 删除 `image-generator.js` 中未使用的 `getFieldLabel` 方法。
- 全站 42 个 JS/HTML 文件版权头版本号从 `1.0.0` 统一更新为 `1.3.3`。
- 源代码文档重新生成：收录文件从 7 个扩展到 73 个（35177 行 / 704 页），覆盖全部业务模块。
- 软件说明书更新：补充多语言系统、活动日志、数据导出功能说明；素材库 API 从 2 个补全到 6 个；新增活动日志、管理员、系统接口文档；目录结构同步为当前实际结构。
- `backend/db/users-db.js`、`backend/providers/jimeng.js` 中 5 处 `console.log` 替换为 `logger.info`，后端日志输出统一。
- `scripts/generate-source-doc.js` 生成模板版本号更新为 V1.3.3。
- 全站 53 个 JS 文件版本号统一为 V1.3.3。
- 补全 8 个文件缺失的版权头（dashboard-init、admin-init、asset-library、project-detail-init 等）。
- CDN 资源本地化：Font Awesome 6.0.0、Google Fonts（Orbitron/Rajdhani）、JSZip 3.10.1 下载至 `vendor/` 目录，11 个 HTML 文件改为本地引用，移除所有外部 CDN 依赖。
- 通知组件去硬编码：演示数据简化为 1 条欢迎消息，新增 `loadFromServer()` 方法预留后端对接。
- 软件说明书同步更新：补充图生图、风格迁移、图片放大、背景去除、批量生成、标签系统、URL 导入、游戏引擎导出、CSRF 防护等新功能说明。
- `escapeHtml` 去重：9 处本地定义统一提取为 `js/html-utils.js` 共享模块，8 个 HTML 文件加载该模块。
- 通知模块优化：`notification-component.js` 4 个 `document.addEventListener('click')` 合并为 1 个事件委托；`notification-manager.js` 添加单例检查。
- `backend/proxy.js` 全部 `console.log/error/warn` 替换为 `lib/logger.js` 统一日志模块，生产环境自动静默。
- 硬编码登录路径消除：`user-storage-scope.js` 暴露 `loginHtmlPath()` 到全局，7 个文件改用该函数。
- 后端 session cookie `secure` 改为动态值 `isProd`，生产环境强制 HTTPS。
- 后端加密密钥生产环境强制检查：未设 `ENCRYPTION_KEY` 时拒绝启动。
- `/api/me/profile` 添加 50KB 大小限制 + 字段白名单验证。
- 项目/素材路由补 `req.params.id` 数字校验，无效返回 400。
- 后端路由活动日志空 catch 块补注释说明意图（auth、projects、asset-library、image-proxy）。
- `backend/db/users-db.js` profile_json 解析空 catch 块补注释说明降级逻辑。

### 修复
- image-generator 阿里云视觉/文生图补 CSRF token，修复提取风格/重新生成提示词 403。
- ai-page-utils fetchImageAsBlob 补 `credentials: 'include'`，修复图片代理 401。
- `asset-library.js` 压缩函数默认值 bug：`=== null` 改为 `== null`，修复未传 quality 时默认值失效。
- **信息泄露修复**：`projects.js` 删除项目路由 `getProject` 未校验 ownership，任意用户可通过 ID 猜测他人项目名称。现改为先校验 `project.user_id === req.currentUser.id`，不存在或无权返回 404。
- **崩溃修复**：`proxy.js` `/api/docs` 端点 `JSON.parse` 未加 try/catch，`openapi.json` 损坏时请求直接崩溃。现加异常捕获，返回 500 错误信息。
- `dashboard-init.js` 补 `'use strict'` 声明。
- `boot-sequence.js` 启动动画版本号从硬编码 `v1.0.0` 改为动态读取配置。
- **XSS 修复**：`asset-library.js` 下载预览 `document.write` 改为 DOM API；相似素材 `innerHTML` 改为 `createElement` + `.textContent`。
- **XSS 修复**：`image-generator.js` 图片预览 `document.write` 改为 DOM API；onclick handler 中 `escapeHtml` 改为 `escapeJsStr`。
- **XSS 修复**：`admin-init.js` 日期字段补 `escapeHtml()`；`action-group.js` 错误消息补 `escapeHtml()`。
- `dashboard-init.js` 项目卡片 `formatDate()` 返回值补 `escapeHtml()`。
- `user-center-page.js` fetch 补 `res.ok` 检查 + 成功提示；5 处 DOM 访问补 null 检查。
- `asset-library.js` fetch 下载补 `response.ok` 检查。
- `login-page.js` 注册数据从 `localStorage` 改为 `sessionStorage`，避免持久存储用户信息。
- `admin.html` 补 `user-storage-scope.js` 引用，修复 `loginHtmlPath` 未定义导致的运行时崩溃。
- 3 个页面 `html-utils.js` 加载顺序修正，确保通知组件转义函数正确初始化。

## 2026-06-02
### 新增
- 素材库搜索/筛选/排序、批量删除、项目详情页素材搜索。
- 活动日志系统（数据库 + API + 用户中心时间线 UI）。
- 游戏引擎导出（Unity SpriteSheet / Godot Atlas / 通用 SpriteSheet）。
- 色彩面板提取（K-means 聚类主色调，点击复制色值）。
- AI 风格迁移（内容图 + 风格参考图，强度可调）。
- 游戏 UI 模板系统（RPG HUD、背包、按钮、小地图、对话框、图标），提示词改为中文。
- 多语言系统（中/英/日/韩），用户中心偏好语言切换。
- 个人信息页新增身份栏（管理员/普通用户），下拉框改为自定义赛博朋克组件。
- 生图权限调整：免费/模拟图片对所有用户开放，即梦/阿里云/腾讯云需绑定 API。
- image-generator.js 生图请求补 CSRF token。

### 优化
- Dashboard 最近项目改为 API 加载 + localStorage 回退。
- project-detail / admin 页补全 Google Fonts + app-shell 壳 + 侧边栏导航。
- 全站内联脚本提取为外部 JS，CSP 移除 `unsafe-inline`。
- 全站 23 个 JS 文件加 `'use strict'`，清理无意义注释。

### 修复
- admin-init / dashboard-init / action-group 补全 escapeHtml，修复 XSS。
- action-group / project-detail / notification-component 补全剩余 XSS 转义。
- action-group / asset-editor 生图请求补 CSRF token，修复三视图/动作组 403。
- asset-library document.write XSS 转义。
- 后端 AI 服务商路由补 CSRF 防护。
- main.js 补缺失 `</span>` 标签、`project.version` 转义。
- fetch 调用补全 try/catch 和 res.ok 检查。
- 密码切换按钮补 `aria-label`，动态图片补 `alt`。
- MutationObserver 加 200ms 防抖，移除前端 debug console.log。
- asset-editor.js 补 `credentials: 'include'`，修复 AI 重绘 401。
- 启动动效字体粗细统一为 400;500;600。

## 2026-06-01
### 修复
- session cookie 因 `secure: true` 在 HTTP 下不生效。
- CSP 移除 `upgrade-insecure-requests`，修复 HTTP 环境资源加载。

### 新增
- 全站补 favicon，新增 `deploy.sh` 部署脚本。

## 2026-05-31
### 重构
- `proxy.js` 路由拆分为 `projects.js`、`asset-library.js`、`admin.js`、`ai-providers.js`。

### 新增
- GitHub Actions CI、Dependabot、CONTRIBUTING.md、SECURITY.md。

### 修复
- `admin.html` HTML 嵌套错误，更新 README。

## 2026-05-30
### 新增
- 登录页赛博朋克视觉改造、启动动画、全息卡片效果。
- 素材库迁移到服务端 SQLite，新增 CRUD API。
- API 分页、图片格式校验、用户角色系统、错误监控、`npm run ci`。

### 优化
- 背景：渐变星云 + 网格 + 扫描线 + 指针光晕（纯 CSS）。
- CSS/JS 外部化（dashboard 725 行 + AI 页 3203 行），CSP 移除 `unsafe-inline`。
- 全站 23 个 JS 文件加 `'use strict'`，清理无意义注释。
- 无障碍（`aria-label`、`prefers-reduced-motion`）、浏览器兼容（`-webkit-backdrop-filter`）。
- 字体统一、视觉一致性修复、删除旧版主题。

### 修复
- `project-detail.js` 数据改为服务端 API 加载，消除双轨制。
- XSS 转义、死链修复、弯引号修复、空值保护、重复初始化。
- 侧边栏死链、重复 CSS 加载、admin body class、Google Fonts 统一。

## 2026-05-29
### 新增
- 用量追踪、API 密钥验证、管理员面板、配额管理。
- 项目/素材服务端存储，前端迁移到 API。
- 密码修改 API、密钥 AES 加密、按用户限流。

### 安全
- SSRF/XSS 防护、session 安全加固、health 端点权限拆分。

## 2026-05-28
### 新增
- 软著申请文档与生成脚本、源码版权头统一、MIT LICENSE。
- 后端单元测试（18 用例）、CSRF Double-Submit Cookie。

### 优化
- AI 生成页拆分为 4 个模块文件，导航改为动态路径。
- `alert()` 统一替换为 `TechUI.toast()`，清理调试日志。

## 2026-05-27
### 新增
- 锁定 Node.js 22 LTS，提取前端共享模块，拆分后端 lib。

### 优化
- 合并 AI 双入口，清理未引用文件，重写 README。

## 2026-05-19
- 登录页品牌样式微调。

## 2026-05-15
### 新增
- 用户自配 API 凭证、ESLint/Prettier/EditorConfig。

### 优化
- 导航补全、CORS 白名单、TechUI 对话框无障碍。

### 修复
- 侧栏路由缺失。

## 2026-04-15
### 新增
- 腾讯云同机部署并打通公网访问。

## 2026-04-14
### 新增
- 云服务器部署接入、部署文档与 PM2 配置。

### 修复
- 项目管理/详情页无响应、素材入库失败。

## 2026-04-13
### 新增
- 画布编辑器（画笔/橡皮/吸色/油漆桶/选区/撤销重做）。
- 角色动作组三视图生成、即梦 Seedream 接入。

### 优化
- 移除 SD Web UI，统一切换即梦。

## 2026-04-12
### 新增
- SQLite 用户数据库、注册/登录/会话、导航栏登出。

## 2026-04-05
### 新增
- Prompt 模式切换、阿里云万相模型选择。

### 精简
- 移除 OpenAI DALL-E、百度文心一格、Stability AI。

## 2026-04-03
### 新增
- 全站 UI Kit、项目详情页素材编辑。

### 优化
- 视觉统一、`prefers-reduced-motion` 动效降级。

## 2026-03-27
### 新增
- 项目管理/AI 生成/素材库链路打通、项目素材本地上传。

## 2026-03-04
### 新增
- AI 生成器三页视图、图生图链路、腾讯云 ImageToImage。

## 2026-03-03
### 新增
- 模块联动、全局滚动动画、角色动作组批量生图与导出。

## 2026-03-02
### 新增
- SD Web UI 集成、调用参数优化、API 失败优雅降级。
