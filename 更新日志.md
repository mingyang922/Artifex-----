# 更新日志

## 2026-05-29

### 新增
- 用量追踪：新增 `usage_logs` 表，记录每次 API 调用。
- API 密钥验证：`POST /api/me/api-settings/validate`。
- 管理员面板：`modules/admin/admin.html`（用户列表、用量统计）。
- 配额管理：`user_quotas` 表，超限返回 429。
- 项目/素材服务端存储：`projects`、`project_assets`、`version_history` 表，前端从 localStorage 迁移到 API。
- 密码修改 API：`POST /api/me/change-password`，真正修改数据库密码。
- API 密钥加密：AES-256-CBC 加密存储，读取时自动解密。
- 按用户限流：`backend/lib/rate-limiter.js`，图片生成每分钟 10 次。
- 共享 API 工具：`js/api-utils.js`（`getCsrfToken` 带缓存）。

### 安全
- SSRF 防护：`/api/proxy-image` 添加 URL 校验，禁止访问内网地址。
- XSS 防护：项目管理页 innerHTML 注入改为 `escapeHtml` 转义。
- session cookie 生产环境强制 `secure: true`。
- `health` 端点拆分：基础版公开，详细版需管理员权限。
- session 有效期从 14 天缩短为 1 天。
- 删除 `users.json`（含密码哈希）。
- 删除假的 2FA 逻辑（验证码、倒计时等）。

### 修复
- 项目详情页「从线稿生成」「从描述生成」「从素材库添加」「统一风格」改为跳转到对应功能页，不再返回假数据。
- 项目描述字段统一：`desc` / `description` 兼容处理。
- 图片生成进度条改为不确定动画，不再显示假百分比。

### 清理
- 删除无用的语言切换功能（`LanguageManager`）。
- 删除多余的 `visual-workbench-page.js`（仅一行重定向）。
- 加密密钥无环境变量时输出警告，不再静默使用默认值。

### 优化
- 用户中心下拉框改为 tech-select 样式。
- 管理员面板视觉优化。
- 图片生成进度条改为不确定动画，不再显示假百分比。
- 内联样式提取为 CSS 类（`.toast-message`、`.custom-tooltip`）。

## 2026-05-28

### 新增
- 软著申请文档：新增 `docs/软件说明书.md`、`docs/源代码文档.txt` 及配套生成脚本 `scripts/generate-source-doc.js`。
- 源码版权头：全部 JS 文件统一版权声明（`[开发者1], [开发者2], [开发者3]` → `Artifex Team`），共修正 33 个文件。
- `js/login-page.js`：从 `login.html` 提取内联脚本（189 行），inline `onclick` 改为 `data-switch-form` 属性。
- MIT `LICENSE` 文件。
- 后端单元测试：`tests/backend.test.js`（18 个用例），使用 Node 22 内置测试运行器，覆盖用户库、鉴权策略、API 凭证存取。

### 安全
- CSRF 防护：新增 Double-Submit Cookie 模式，状态变更接口均校验 `X-XSRF-Token`。
- 会话固定防护：登录/注册后 `req.session.regenerate()` 重新生成会话 ID。
- XSS 防护：通知组件渲染时转义 HTML 特殊字符。
- SSRF 修复：`/api/proxy-image` 补充 `requireAuth`。
- 认证接口限流：`express-rate-limit` 限制注册/登录每 15 分钟最多 20 次请求。

### 修复
- 修复 AI 生成页「图片生成」「动作组生成」Tab 无响应：`GameUiUserScope.ensure()` 返回值检查导致初始化中断。
- 修复 `notification-manager.js` 中 `JSON.parse` 缺少 try-catch。

### 优化
- 前端模块拆分：`ai-generator-page.js`（3812 行）拆分为 `action-group.js`（586）、`asset-editor.js`（1181）、`image-generator.js`（1243）及主文件（798），HTML 同步更新加载顺序。
- 导航重构：`navigation.js` 改为动态路径计算，新增页面仅需维护 `modulePaths` 映射，不再逐页更新路由表。
- `backend/db/users-db.js` 支持 `USERS_DB_PATH` 环境变量，便于测试使用临时数据库。
- `alert()` 统一替换为 `TechUI.toast()`。
- 清理前端 `console.log`：`navigation.js`、`notification-*.js`、`avatar-manager.js`、`user-center-page.js`、`ai-generator-page.js`、`messageCenter.html`、`index.html`、`asset-library.html`。
- 清理后端调试日志：`tencent.js` 移除完整响应输出，`alibaba.js` 移除轮询日志，`proxy.js` 配置日志仅开发环境输出。
- `.gitignore` 补充 `backend/data/*.sqlite*`、`backend/data/users.json`、`logs/`、`artifex-deploy.tgz`、`.claude/`、`.vscode/`。
- 删除重复 `backend/package.json`，清理 `crypto` 内置模块依赖，同步依赖版本。

## 2026-05-27

### 新增
- 锁定 Node.js 22 LTS：`.nvmrc`、`.node-version`、`.npmrc`、`package.json#engines`，`preinstall`/`postinstall` 校验版本。
- 提取前端共享模块：`js/page-effects.js`、`js/ai-page-utils.js`、`modules/project-management/pm-shared.js`。
- 拆分后端：从 `proxy.js` 提取 `backend/lib/auth-policy.js`、`backend/lib/user-api-settings.js`。

### 优化
- 合并 AI 双入口：`visual-workbench.html` 改为兼容跳转。
- 收敛 AI 脚本：主逻辑统一在 `ai-generator-page.js`。
- 清理未引用文件，重写 `README.md`。

## 2026-05-19

- 登录页品牌样式微调。

## 2026-05-15

### 新增
- 用户登录后自配 API 凭证链路。
- `free/mock` 提供商调整为管理员可用。
- 引入 ESLint / Prettier / EditorConfig。

### 优化
- 导航补全、品牌文案对齐、大脚本改为外链 `defer`。
- 生产环境强制 `SESSION_SECRET` 与 `ALLOWED_ORIGINS`，CORS 白名单化。
- `TechUI` 对话框 ARIA/焦点陷阱，`tech-select` 键盘无障碍。

### 修复
- 侧栏「账户」路由缺失，AI 页侧栏字体不一致。

## 2026-4-15

### 新增
- 腾讯云同机部署并打通公网访问。

### 修复
- Linux 环境依赖与 Nginx 配置问题，DashScope 密钥接入验证。

## 2026-4-14

### 新增
- 云服务器部署环境接入。
- AI 生成页居中主题成功提示。
- 部署文档 `docs/DEPLOY_TENCENT_SINGLE_SERVER.md`、PM2 配置 `ecosystem.config.js`、环境变量模板 `.env.example`。

### 优化
- 文件上传控件与通知滚动条统一主题色。
- 项目管理空状态交互优化。
- 环境变量加载策略：优先 `backend/.env`，兼容 `config/.env`。
- 前端 API 统一同源 `/api/*`。

### 修复
- 项目管理/详情页点击无响应、素材入库失败、素材库空数据、通知叠层冲突。
- Linux 下 `better-sqlite3` 编译失败、Nginx 500 错误。

## 2026-4-13（续二）

### 新增
- 「代码生成」重构为「资产微调」：画布编辑器，支持画笔、橡皮、吸色、油漆桶、选区、缩放、撤销/重做、导出 PNG。
- 画布像素编辑辅助：参考网格、像素坐标、RGBA 信息。
- 动作组结果保存入口：三视图/Spritesheet/逐帧保存到素材库。

### 优化
- 画布改为真正像素级处理（Bresenham 逐像素绘制、禁用抗锯齿）。
- 历史记录改为 `ImageData` 快照，油漆桶新增容差滑块。

## 2026-4-13（续）

### 新增
- 角色动作组三视图生成链路，支持 PNG/ZIP 下载与参数元数据。
- 图片大图预览（暗色遮罩、Esc 关闭）。
- 即梦 Seedream 服务商接入。
- `/api/health` 新增配置状态与 provider 列表。

### 优化
- 移除 SD Web UI 入口，统一切换即梦。
- 动作提示词与动作类型改为互斥输入。
- 三视图自动裁切正面参考，动作组加强单角色约束。
- 后端统一错误格式，前端 `showApiError` 分类提示。

### 修复
- 三视图附加提示词校验、即梦尺寸校验、动作组偏离参考、多人/镜像帧、API 错误提示不一致。

## 2026-4-12

### 新增
- 接入 SQLite 用户数据库，支持注册/登录/会话。
- 导航栏登出功能，点击头像进入用户中心。

### 修复
- 通知按钮重叠、头像跨用户污染、默认头像显示、UI 细节统一。

## 2026-4-5（续三）

### 精简
- 移除 OpenAI DALL-E、百度文心一格、Stability AI。

### 修复
- 万相/千问模型选择框显示问题（tech-select wrapper 可见性）。

## 2026-4-5（续二）

### 优化
- 「专业 UI Prompt」改为两步流程：先生成可编辑 Prompt，确认后再生图。
- 万相模型选择框仅选择阿里云时显示。

## 2026-4-5（续）

### 新增
- Prompt 模式切换（个性需求 / 专业 UI Prompt）。
- 阿里云万相模型选择（wanx-v1 / turbo）。

## 2026-4-5

### 修复
- 阿里云 API 环境变量名兼容（`DASHSCOPE_API_KEY` / `ALIBABA_API_KEY`）。
- 万相文生图改为异步轮询，尺寸自动映射。
- API 失败不再静默降级为假图。

## 2026-4-3

### 新增
- 全站 UI Kit：`js/ui-kit.js`、`styles/ui-kit.css`。
- 项目详情页素材编辑。

### 优化
- 视觉统一、布局对齐、`prefers-reduced-motion` 动效降级。

### 修复
- 原生提示框改为主题弹窗，素材编辑弹窗溢出。

## 2026-3-27

### 新增
- 项目管理/AI 生成/素材库链路打通。
- 项目素材本地上传。

### 优化
- 图片压缩与体积控制，首版主题视觉。

### 修复
- 素材上传未保存、风格选择校验错误。

## 2026-3-4（续）

### 新增
- 图生图链路（线稿→成品），后端接入腾讯云 ImageToImage。

## 2026-3-4

### 新增
- AI 生成器三页视图：代码生成、图片生成、角色动作组。

### 优化
- Tab 切换动效。

## 2026-3-3

### 新增
- 模块联动与导航快捷入口。
- 全局滚动动画与页面切换过渡。
- 角色动作组：批量生图、Spritesheet/TXT/JSON/ZIP 导出。

### 优化
- AI 生成器样式统一，代码表单布局优化。

## 2026-3-2

### 新增
- SD Web UI 集成（文生图/图生图）。

### 优化
- 调用参数优化，API 失败优雅降级。
