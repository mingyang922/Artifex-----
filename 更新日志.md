# 更新日志

## 2026-05-30
### 新增
- 登录页赛博朋克视觉改造：动态渐变星云、网格叠加、玻璃拟态卡片、光带扫过。
- 登录成功后启动动画（Orbitron/Rajdhani 品牌展示 + 进度条）。
- 全息卡片效果：鼠标悬停 3D 倾斜 + 彩虹色偏移。
- 素材库迁移到服务端 SQLite，新增 `asset_library` 表及 CRUD API。
- API 分页：`GET /api/projects` 和 `GET /api/asset-library` 支持 `?page=&limit=`。
- 素材上传图片格式校验：验证 MIME 类型与 magic bytes。
- 用户角色系统：`users` 表新增 `role` 列，管理员判断改为数据库角色制。
- 前端全局错误监控：`window.onerror` + `unhandledrejection` 结构化日志。
- `npm run ci` 一键 lint + test。

### 优化
- 背景层次：渐变星云 + 网格线 + 扫描线 + 指针光晕，纯 CSS 实现。
- 全站 16 个 JS 文件加 `'use strict'`。
- Dashboard 内联 725 行 CSS 外部化为 `styles/dashboard.css`。
- AI 生成页内联 3203 行 CSS 外部化为 `modules/ai-generate/style.css`。
- 全站内联脚本提取为外部 JS（8 个 HTML 文件），CSP 移除 `unsafe-inline`。
- project-detail / admin 页补全 Google Fonts + app-shell 壳 + 侧边栏导航。
- 复选框/单选框自定义赛博朋克样式。
- 白色边框→青色边框，卡片悬停 cyan 光晕，空状态虚线边框。
- 无障碍：图标按钮 `aria-label`、搜索框标签、`prefers-reduced-motion` 支持。
- 浏览器兼容：`backdrop-filter` 补 `-webkit-` 前缀。
- `docs/openapi.json` 同步最新 API。
- 删除旧版金色主题 `styles.css`。
- messageCenter 页面视觉升级：Google Fonts、赛博朋克配色、玻璃拟态卡片。

### 功能
- `project-detail.js` 改为从服务端 API 加载数据，消除 localStorage/服务端双轨制。
- `main.js` 错误处理：加载/保存/删除失败时弹出提示，删除操作改为服务端确认。
- `image-generator.js` / `project-detail.js` innerHTML 注入加 XSS 转义。
- Dashboard 最近项目改为 API 加载 + localStorage 回退，项目变更实时同步。
- 通知中心死链修复：`messageCenter.html` → `userCenter.html`。
- 忘记密码/通知系统标注为演示功能。
- 服务端素材接口加 10MB 体积限制。
- 全站清理无意义注释。
- 修复 6 个文件弯引号 `"` → 直引号 `"` 导致脚本 404。

### 修复
- `project-detail.js` 未定义变量 `currentProjectId` → `currentProject.id`。
- `main.js` 异步竞态：`initModule()` 补 `await`，移除重复调用。
- `userCenter.html` CSS 嵌套语法修复、移除重复定义。
- `action-group.js` `getElementById` 补可选链空值保护。
- `login.html` Logo 补 `alt` 属性。
- `package.json` 移除未使用的 `jest`/`vitest`。
- `ai-generator-page.js` DOM 访问移入 `DOMContentLoaded`。
- `avatar-manager.js` 移除重复初始化。
- admin 侧边栏 3 个死链修正。
- messageCenter 内联脚本提取为外部 JS，删除残留 HTML。
- ai-generator-new 补全"账户"导航链接。
- 删除未使用的 `particle-network.js` 及相关 CSS。

## 2026-05-29

### 新增
- 用量追踪表、API 密钥验证、管理员面板、配额管理。
- 项目/素材服务端存储，前端迁移到 API。
- 密码修改 API、API 密钥 AES 加密、按用户限流。
- 共享 API 工具 `js/api-utils.js`。

### 安全
- SSRF/XSS 防护、session cookie 安全加固、health 端点权限拆分。
- session 缩短为 1 天，删除 `users.json` 和假 2FA 逻辑。

### 修复
- 项目详情页四项操作改为跳转功能页，不再返回假数据。
- 项目描述字段 `desc`/`description` 兼容。
- 进度条改为不确定动画。

### 清理
- 删除无用语言切换、多余 `visual-workbench-page.js`。
- 加密密钥无环境变量时输出警告。

## 2026-05-28

### 新增
- 软著申请文档与生成脚本、源码版权头统一、MIT LICENSE。
- 后端单元测试（18 用例）。
- `login.html` 内联脚本提取为 `login-page.js`。

### 安全
- CSRF Double-Submit Cookie、会话固定防护、XSS 转义、SSRF 补全鉴权。
- 注册/登录限流。

### 优化
- AI 生成页拆分为 4 个模块文件，导航改为动态路径计算。
- `alert()` 统一替换为 `TechUI.toast()`。
- 清理前后端调试日志，`.gitignore` 补全。

### 修复
- AI 生成页 Tab 无响应、`notification-manager.js` JSON.parse 缺 try-catch。

## 2026-05-27

### 新增
- 锁定 Node.js 22 LTS，提取前端共享模块，拆分后端 lib。

### 优化
- 合并 AI 双入口，收敛脚本，清理未引用文件。

## 2026-05-19

- 登录页品牌样式微调。

## 2026-05-15

### 新增
- 用户自配 API 凭证、`free/mock` 提供商调整、ESLint/Prettier/EditorConfig。

### 优化
- 导航补全、品牌文案对齐、大脚本 `defer`。
- 生产环境强制 `SESSION_SECRET`/`ALLOWED_ORIGINS`，CORS 白名单化。
- TechUI 对话框 ARIA/焦点陷阱，tech-select 键盘无障碍。

### 修复
- 侧栏「账户」路由缺失，AI 页侧栏字体不一致。

## 2026-04-15

### 新增
- 腾讯云同机部署并打通公网访问。

### 修复
- Linux 依赖与 Nginx 配置，DashScope 密钥接入。

## 2026-04-14

### 新增
- 云服务器部署接入、部署文档与 PM2 配置。

### 优化
- 文件上传控件主题色、项目管理空状态、环境变量加载策略、API 统一同源。

### 修复
- 项目管理/详情页无响应、素材入库失败、通知叠层冲突。
- Linux `better-sqlite3` 编译、Nginx 500。

## 2026-04-13（续二）

### 新增
- 「代码生成」重构为「资产微调」画布编辑器（画笔/橡皮/吸色/油漆桶/选区/缩放/撤销重做/导出）。
- 画布像素编辑辅助、动作组结果保存到素材库。

### 优化
- 画布像素级处理、历史记录改为 ImageData 快照、油漆桶容差滑块。

## 2026-04-13（续）

### 新增
- 角色动作组三视图生成、图片大图预览、即梦 Seedream 接入、health 端点扩展。

### 优化
- 移除 SD Web UI，统一切换即梦。三视图自动裁切，动作组单角色约束。

### 修复
- 三视图/即梦/动作组多项校验与提示问题。

## 2026-04-12

### 新增
- SQLite 用户数据库、注册/登录/会话、导航栏登出与头像入口。

### 修复
- 通知按钮重叠、头像跨用户污染、默认头像显示。

## 2026-04-05（续三）

### 精简
- 移除 OpenAI DALL-E、百度文心一格、Stability AI。

### 修复
- 万相/千问模型选择框显示。

## 2026-04-05（续二）

### 优化
- 「专业 UI Prompt」改为两步流程，万相模型选择框条件显示。

## 2026-04-05（续）

### 新增
- Prompt 模式切换、阿里云万相模型选择。

## 2026-04-05

### 修复
- 阿里云 API 环境变量名兼容、万相文生图异步轮询、API 失败不再降级假图。

## 2026-04-03

### 新增
- 全站 UI Kit、项目详情页素材编辑。

### 优化
- 视觉统一、`prefers-reduced-motion` 动效降级。

### 修复
- 原生提示框改为主题弹窗，素材编辑弹窗溢出。

## 2026-03-27

### 新增
- 项目管理/AI 生成/素材库链路打通、项目素材本地上传。

### 优化
- 图片压缩与体积控制，首版主题视觉。

### 修复
- 素材上传未保存、风格选择校验错误。

## 2026-03-04（续）

### 新增
- 图生图链路，后端接入腾讯云 ImageToImage。

## 2026-03-04

### 新增
- AI 生成器三页视图：代码生成、图片生成、角色动作组。

### 优化
- Tab 切换动效。

## 2026-03-03

### 新增
- 模块联动与导航快捷入口、全局滚动动画与页面切换过渡。
- 角色动作组：批量生图、Spritesheet/TXT/JSON/ZIP 导出。

### 优化
- AI 生成器样式统一，代码表单布局优化。

## 2026-03-02

### 新增
- SD Web UI 集成（文生图/图生图）。

### 优化
- 调用参数优化，API 失败优雅降级。
